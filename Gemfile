source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in gager-cli.gemspec
gemspec

gem "gager-core", github: "jonaskay/gager-core"
